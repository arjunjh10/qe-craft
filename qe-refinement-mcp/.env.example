# Copy to .env (gitignored). Never commit .env.
# Each developer uses their own Anthropic key (BYOK).
ANTHROPIC_API_KEY=
# Optional: repo where docs/qe-analysis/ should be written (defaults to cwd)
REPO_ROOT=
