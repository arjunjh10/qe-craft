# Changelog

All notable changes to the QE Craft plugin bundle are documented here. Plugin version tracks the `@qe-craft/mcp` npm release.

## 1.0.1

- Initial plugin bundle: Cursor + Claude manifests, MCP config, skills synced from `mcp/skills/`.
